import React from "react";
import { RouteProps } from "../../routes/routeTypes";

const CreateActivity = ({ userInfo }: RouteProps) => {
  return <h3>Create Activity</h3>;
};

export default CreateActivity;
